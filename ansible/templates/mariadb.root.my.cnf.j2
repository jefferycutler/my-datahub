# Ansible-managed: MariaDB root client credentials
# Used by the CLI and by community.mysql Ansible modules on this host.
[client]
user     = root
password = {{ mariadb_root_pw }}
socket   = /var/run/mysqld/mysqld.sock
